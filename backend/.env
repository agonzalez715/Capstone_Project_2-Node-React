# ── OMDb API Key ───────────────────────────────────────────────
# Used by your search route to authenticate requests against the OMDb API.
# Kept out of your code so you don’t accidentally check it into version control.
OMDB_API_KEY=6a2428ae

# ── PostgreSQL Connection String ─────────────────────────────────
# Tells Sequelize (and pg) how to connect to your local Postgres database.
# Format: postgres://<host>/<database_name>
DATABASE_URL=postgres://localhost/movie_reviews